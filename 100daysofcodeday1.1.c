#include <stdio.h>
void main(){
    int a, b, sum;
    //take input from user
    printf("enter first numbers:");
    scanf("%d", &a);
    printf("enter second numbers:");
    scanf("%d", &b);
    //calculate sum
    sum = a+ b;
    //display the result
    printf("sum of %d and %d is: %d\n", a, b, sum);


}