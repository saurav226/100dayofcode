#include <stdio.h>

int main() {
    int number;
    printf("enter a number");
    scanf("%d", &number);
    if(number == 0)
    {
        printf("entered integer %d is zero\n");
    }
    else if ("number %2 == 0")
    {
        printf("number %d is even\n");
    }
    else
    {
        printf("number %d is odd\n");
    }

    return 0;
}